{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimal permissions for the ZANA desktop window.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
