# T004 Integration Handoff — Reproducible Authenticated Desktop Foundation

Verdict: PASS

## Integrated outcome

- Merged the DeepSeek V4 Pro Core lane and the desktop lane without overlapping
  ownership or discarding either handoff.
- Corrected the lane boundary before validation: the desktop now decodes the
  Core's exact `status`, `version`, `python_version`, `pid`, and
  `uptime_seconds` fields and supplies `ZANA_CORE_TOKEN` to the sidecar.
- Replaced the packaging stub with a real PyInstaller build that emits the
  target-qualified sidecar expected by Tauri.
- Added reproducible pnpm, uv, and Cargo lockfiles and a generated platform icon
  set from the project-owned ZANA application icon.
- Moved per-launch Core configuration from a Python module global to FastAPI
  application state after the frozen binary smoke exposed duplicate-module
  state under PyInstaller.

## Verification evidence

| Check | Result |
| --- | --- |
| Core pytest | 7 passed |
| Ruff lint + format | pass |
| Pyright | 0 errors, 0 warnings |
| Desktop ESLint + TypeScript | pass |
| Vitest | 3 passed |
| Vite production build | pass |
| Cargo fmt + clippy `-D warnings` | pass |
| PyInstaller target sidecar | built and installed |
| Frozen sidecar authorized health | HTTP 200 with real pid/version/Python/uptime |
| Frozen sidecar missing and wrong token | HTTP 401 canonical envelope |
| Tauri debug bundle | `ZANA.app` built successfully |
| Actual desktop launch | visible `CORE CONNECTED`, real sidecar pid 59745 |
| Desktop quit | parent and sidecar exited cleanly |

## Security delta

- Core binds only to loopback and rejects missing, malformed, empty, and wrong
  bearer credentials using constant-time comparison.
- The random token is per launch, passed only by environment/invoke, and never
  persisted or logged.
- No telemetry client remains in the resolved Python environment.
- Web development fails closed without an explicit base URL and token.

## Generated brand asset

Built-in image generation produced the project-owned source icon using this
direction: a premium graphite, warm-white, and oxidized-copper folded aperture
for a local-first AI capability studio, with no text or watermark. Tauri's icon
generator derived the platform asset set under `apps/desktop/src-tauri/icons/`.

## Residual risk / next contract gate

- Disk headroom is approximately 3.4 GiB after native dependency compilation;
  later model/runtime acquisition must preserve the recorded safety reserve.
- The current screen is intentionally only the verified M0 system surface.
  Product feature navigation must not fan out until T005 settles shared domain,
  database, job, API, and generated-client contracts.

## Blockers

None for M0.
