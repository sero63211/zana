# Founder UI Launcher

Copy this into a new Codex task in the saved **ZANA** project. Select a fresh
local isolated worktree.

```text
/goal Read and execute /Users/sero/Documents/zana/docs/desktop-frontend-program/ui-agent-prompt.md in full. Use /Users/sero/Documents/zana/docs/desktop-frontend-program/ui-execution-brief.md as the exact UI/product specification and read the rest of /Users/sero/Documents/zana/docs/desktop-frontend-program/ only as routed by those files. Build the real concise English ZANA desktop UI end-to-end in a fresh local isolated worktree. Use exactly opencode-go/deepseek-v4-flash with reasoning max; never DeepSeek V4 Pro. Do not reuse or integrate the stopped partial worktree /Users/sero/.codex/worktrees/a473/zana unless I explicitly authorize recovery later. Obey AGENTS.md and GoalBuddy ownership, use only real accepted contracts, add no fake data or dependency, and run only the focused low-memory checks allowed by the prompt. Notify the lead only on BLOCK, INTERFACE, or COMPLETE; do not push.
```

The launcher is intentionally short. The complete implementation contract is
in [`ui-agent-prompt.md`](ui-agent-prompt.md); the exact visual/product brief is
in [`ui-execution-brief.md`](ui-execution-brief.md).
